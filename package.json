{
  "name": "split",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack Vite React and Express application.",
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,magenta \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gusatb/split.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/gusatb/split/issues"
  },
  "homepage": "https://github.com/gusatb/split#readme",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
